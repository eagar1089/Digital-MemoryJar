---
title: Digital Memory Jar Backend
emoji: 🧠
colorFrom: green
colorTo: blue
sdk: docker
pinned: false
---

Digital Memory Jar backend deployed as a Hugging Face Docker Space.

The Space root is populated by the deploy workflow from the repository's
`backend/` directory, with the top-level `Dockerfile` and this README copied
into place so Hugging Face can detect the Docker configuration correctly.